{
  "license": "MIT",
  "scripts": {
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "test": "yarn docker:up && yarn prisma migrate dev && jest -i --verbose"
  },
  "devDependencies": {
    "@jest/globals": "^29.4.2",
    "@types/node": "^18.13.0",
    "jest": "^29.4.2",
    "prisma": "^4.10.1",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "@prisma/client": "^4.10.1",
    "@types/jest": "^29.4.0",
    "ts-jest": "^29.0.5"
  }
}
